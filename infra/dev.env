# rename this doc to .dev
# change the constants below to the real ones

SECRET_KEY = 'some_key'
DB_ENGINE='some_engine' # most likely django.db.backends.postgresql
DB_NAME='some_name'
POSTGRES_USER='some_user'
POSTGRES_PASSWORD='some_password'
DB_HOST='some_host'
DB_PORT=1111 # some_port